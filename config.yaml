use_case: "stage_1_test"

data:
  path: "data/raw/sample_data.csv"
  features:
    - metric_1
    - metric_2
    - metric_3

anomaly_detection:
  alpha: 0.1

operational:
  system: "inventory"
  region: "north"
  owner_team: "supply_chain"
